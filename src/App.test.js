import { render } from '@testing-library/react';
import App from './App';

test('Base test', () => {
  let checkfunc = document.getElementsByClassName("Button");
  expect(1).toEqual(1);
});

//Add more tests if necessary

